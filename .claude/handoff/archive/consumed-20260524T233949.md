---
saved-at: 2026-05-24T18:06:39Z
saved-at-sha: 7be7acf
saved-at-branch: feat/visual-companion-plugin
saved-from-cwd: /Users/apurvbazari/Desktop/projects/claude-plugins
---

# Pick up the visual-companion v0.2 brainstorm at the user-review gate

We just finished a long brainstorm on the v0.2 architecture for `visual-companion/skills/render-adr/`. The spec is written and self-reviewed; the user paused before reviewing it. **Resume by asking the user to review the spec, then transition to `writing-plans` per the brainstorming skill's terminal state.**

## What to pick up

1. Read the spec first: `docs/superpowers/specs/2026-05-24-visual-companion-v2-architecture-design.md` (943 lines). NOTE: `docs/` is gitignored at repo root — the file exists locally but isn't tracked. User had three options to consider (leave gitignored / add negation rule / `git add -f`); not yet decided.
2. Ask the user to review the spec. If "looks good" → invoke `superpowers:writing-plans` to produce the implementation plan. If "tweak X" → revise the spec, re-self-review, re-ask. Do NOT skip the review gate.
3. The original brainstorm artifacts (16 visual mockups) live at `.superpowers/brainstorm/24927-1779630862/content/` for reference but are not authoritative — the spec is.

## Constraints — do NOT relitigate

All locked decisions, listed once:

- Layers C → B → A; this spec is C+B only.
- **Medium composability** — shared theme + component library at `visual-companion/components/`, hand-assembled per-stage renderers.
- **13-component library** in v0.2 — chrome (meta-header, toc-sidebar), sections (decision, alternatives, open-questions, consequences, context, related, files-affected), inline (decision-tree, mermaid-panel, comparison-matrix, before-after) + generic-card fallback + detail-panel primitive.
- **3-tier section detection** — canonical heading aliases (Tier 1) → `{type=…}` attribute (Tier 2) → generic-card fallback (Tier 3).
- **4 modes** — A (file), B (session), C (focused prompt), D (auto-upgrade integrated into render-adr; NOT a separate skill, NOT a `--upgrade` flag). D-lite scope: body content never touched.
- **Frontmatter** — flat AND nested `adr.*` form both supported; `sections_override:` alongside `{type=…}`.
- **Renderer = 100% deterministic Node**; Claude participates ONLY in synthesis (B/C/D).
- **frontend-design** — authoring-time only, no runtime dep.
- **Canonical-headings catalog extended** with dev-doc aliases (Problem / Root cause / Goals / Non-goals / Design / Affected files) from real specs analyzed in villasa + notifyguard.

## Open questions parked for the plan doc

Five items in the spec's Open Questions section (inline-JS bundling, snapshot-test framework, `.adr-aliases.json` location, Mode D post-adoption flow, synthesizer cost telemetry). Don't try to resolve them now — they belong in the plan or v0.3.

## State expectations at resume

- Branch `feat/visual-companion-plugin` at `7be7acf`; no commits made during the brainstorm. Working tree dirty: `.gitignore` modified (blanket `.claude/`); spec file untracked under gitignored `docs/`.
- Brainstorm server stopped.
- No PR open.

Related memory: [[greenfield-briefs-design]] (set the "frontend-design timing: hybrid" precedent we adopted as authoring-time only).
