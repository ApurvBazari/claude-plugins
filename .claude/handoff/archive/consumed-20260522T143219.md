---
saved-at: 2026-05-22T08:53:03Z
saved-at-sha: 5495fa1
saved-at-branch: feat/html-adr
saved-from-cwd: /Users/apurvbazari/Desktop/projects/claude-plugins
---

Read `docs/superpowers/plans/2026-05-22-html-adr-hardening-implementation.md` and execute commit A first via `superpowers:subagent-driven-development`. Each of commits A–G is self-contained with exact code, exact bash commands, and expected output per step.

**Constraints**

- Do NOT relitigate the design. Five hardenings (markers + script-data validator + Mermaid securityLevel guard + Puppeteer smoke + CLAUDE.md docs) are locked. The spec at `docs/superpowers/specs/2026-05-22-html-adr-hardening-design.md` explains the rejected alternatives — L1 (Chrome extension tamper detection) dropped, L2 folded into L5, `shell.html` deliberately untouched because `wrapBundle` newlines handle script isolation.
- Stay on `feat/html-adr`. Do not push or open a PR until all 7 commits land AND both `npm test` and `npm run test:smoke` (from inside `html-adr/`) are green.
- Goldens MUST stay byte-stable. If any of the 6 existing golden tests fails after commit B, the `stripMarkers` wiring in `tests/render-e2e.test.mjs`'s `normalize()` is wrong — fix that, do NOT regenerate goldens via `UPDATE_GOLDEN=1`.

**Verify at resume**

- Branch is `feat/html-adr`, working tree clean.
- HEAD is at or just past `5495fa1` (the plan commit). If far past, someone (or a prior session) already started; read `git log -10` first to find which commits A–G are already in.
- `html-adr/package.json` is still version `0.1.0`. The v0.2.0 bump is a SEPARATE release commit, not part of this work.

**On completion of commit G**

Do not bump `html-adr/CHANGELOG.md` or `package.json` version in this branch. The v0.2.0 release roll-up happens separately and will fold in other v0.2 inputs alongside this hardening.

Open questions: none. Plan is fully specified.
