---
saved-at: 2026-05-24T11:40:23Z
saved-at-sha: a4e2c49
saved-at-branch: develop
saved-from-cwd: /Users/apurvbazari/Desktop/projects/claude-plugins
---

## Pick up

Execute the **visual-companion plugin v0.1.0** implementation plan via **subagent-driven-development**. Start at Phase 1, Task 1. The plan has full code in every step — do not re-derive.

## Constraints (read carefully)

- **Use Opus for every subagent.** The user explicitly chose `model: "opus"` for all subagent dispatches in this implementation. Do NOT default to Sonnet/Haiku, even on simple tasks like file scaffolding or commit messages. Pass `model: "opus"` on every `Agent` tool call in `subagent-driven-development`.
- The plan is authoritative — do NOT re-design or re-architect mid-execution. If a task surfaces a real problem, pause and ask the user before deviating.
- Do NOT commit the spec or the plan files. They live in `docs/` which is gitignored per repo policy, and the user explicitly asked to keep both uncommitted during the design phase.
- Do NOT push the branch or open a PR. Task 20 Step 7 documents the push + `gh pr create` command, but it is gated on explicit user authorization at the end.

## Pointers

- **Plan**: `docs/superpowers/plans/2026-05-24-visual-companion-plugin.md` (~60 KB, 20 tasks across 10 phases, TDD where applicable)
- **Spec**: `docs/superpowers/specs/2026-05-24-visual-companion-plugin-design.md` (~22 KB; the design conclusions the plan implements)
- **Repo conventions**: root `CLAUDE.md` (plugin structure, frontmatter spelling, conventional-commit scope)
- **Existing plugin patterns to mimic**: `handoff/`, `notify/`, `onboard/` — folder layouts, manifest fields, README/CLAUDE.md style

## State expectations at resume

- On `develop` branch with clean working tree (last commit `a4e2c49`)
- Branch `feat/visual-companion-plugin` does **not** exist yet — Task 1 Step 2 creates it
- Orphan empty `html-adr/` directory still at repo root (`node_modules/` + empty subdir, nothing tracked) — Task 1 Step 4 removes it
- No `visual-companion/` code exists yet; Task 2 creates the full scaffold
- `.claude/handoff/` and `.superpowers/` are gitignored — never commit those

## Open

None. Self-review pass on both spec and plan found and fixed one inconsistency (deferred-skill naming) inline. Plan is complete; engineer needs no further research.
