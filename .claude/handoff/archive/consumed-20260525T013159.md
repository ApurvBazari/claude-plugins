---
saved-at: 2026-05-24T19:09:33Z
saved-at-sha: 7be7acf
saved-at-branch: feat/visual-companion-plugin
saved-from-cwd: /Users/apurvbazari/Desktop/projects/claude-plugins
---

# Execute visual-companion v0.2 Session 1 (Phases 1-4)

Spec approved, plan written. Start executing Session 1 of the 3-session subagent-driven implementation.

## What to pick up

Invoke `superpowers:subagent-driven-development` with the plan at `docs/superpowers/plans/2026-05-24-visual-companion-v2-implementation.md`. Execute **Phases 1 through 4 only** (Tasks 1.1 → 4.4). This is Session 1 of 3.

## Pointers

- **Plan:** `docs/superpowers/plans/2026-05-24-visual-companion-v2-implementation.md` (under gitignored `docs/`)
- **Spec:** `docs/superpowers/specs/2026-05-24-visual-companion-v2-architecture-design.md` (source of truth for design decisions)
- **Memory:** `[[project-visual-companion-v2]]` has the 3-session breakdown
- **Memory:** `[[feedback-opus-for-subagents]]` — use `model: "opus"` on ALL Agent tool calls

## Constraints

- **Opus for all subagents.** User explicitly requested this. Pass `model: "opus"` on every Agent dispatch.
- **Session 1 scope = P1-P4 only.** Do NOT continue into Phase 5+. Those are Session 2.
- **Do NOT relitigate spec decisions.** The 5 open questions are resolved in the plan header. All architectural choices are locked.
- **Commit per task** — one logical commit at the end of each task per the plan's convention.

## State expectations

- Branch `feat/visual-companion-plugin` at `7be7acf`. No commits made since the brainstorm.
- Working tree dirty: `.gitignore` modified (blanket `.claude/`).
- `docs/` is gitignored — spec and plan exist locally but are not tracked (user decided to leave gitignored).
- No PR open.

## Session 1 exit gate

After Task 4.4, run `npm test` from `visual-companion/`. All unit + component tests should pass. Save a handoff for Session 2 (P5-P7).
