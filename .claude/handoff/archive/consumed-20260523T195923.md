---
saved-at: 2026-05-23T14:27:53Z
saved-at-sha: 69baba5
saved-at-branch: feat/html-adr
saved-from-cwd: /Users/apurvbazari/Desktop/projects/claude-plugins
---

Resume the html-adr thin-renderer pivot (v0.3.0). Execute the plan in `docs/superpowers/plans/2026-05-23-html-adr-thin-renderer-pivot.md` — 46 tasks across 12 phases, self-contained with full code in each TDD step. Spec context (read first if anything in the plan looks ambiguous): `docs/superpowers/specs/2026-05-23-html-adr-thin-renderer-pivot-design.md`. Both files live under root `docs/` which is `.gitignored` — that is by design, do not propose moving them or committing them.

Start at **Task 1: fix `textOf()` to handle `inlineCode` in `html-adr/scripts/plugins/widgets/_shared.mjs:7-12`**. Tasks 1–6 (Phase 1) are independent low-risk bug fixes and can each ship as their own commit before any structural changes begin.

Execution mode is not yet picked. The plan offers two options — subagent-driven (recommended for this size) or inline execution. Ask the user which they want before starting Task 1.

Constraints:

- The plan was reached after a long brainstorm that locked the **thin-renderer philosophy** (Approach 1B in the spec's §3): drop 8 styled-prose widgets, drop `sections_override` / `icons` / `chart_col` frontmatter, replace with `<!-- adr:* -->` directives, keep 5 high-value widgets activated by content-detect or directive. Do NOT relitigate this scope — it was chosen against alternatives and the user confirmed via SWOT.
- Branch `feat/html-adr` is **51 commits ahead of origin** at sha `69baba5`. Do NOT push without explicit user confirmation.
- Validation target is `/Users/apurvbazari/Desktop/projects/notifyguard/docs/superpowers/specs/2026-05-22-full-notification-data-capture-design.md` (the sample that surfaced the original 10 bugs). The final task (Task 46) re-renders this and confirms each bug is resolved.
- Working tree is clean at save time. Any uncommitted state at resume belongs to the user; investigate before discarding.
- The 8 widget deletions in Phase 4 (Tasks 13–20) must each be a separate commit per the spec's rollback strategy — do not consolidate.

If the user is in a fresh session: read this file, then the plan, then ask which execution mode. If they want to deviate from the plan, surface the divergence explicitly before acting.
