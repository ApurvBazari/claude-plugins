---
saved-at: 2026-05-21T15:56:45Z
saved-at-sha: a4e2c49
saved-at-branch: feat/html-adr
saved-from-cwd: /Users/apurvbazari/Desktop/projects/claude-plugins
---

Pick up implementation of the new `adr` plugin — a Claude Code plugin that renders a markdown design spec into a single self-contained interactive HTML page (Node + remark/rehype, vendored Cytoscape + Mermaid + dagre + highlight.js, output is offline-portable).

**Read first, in this order:**
1. `docs/superpowers/specs/2026-05-20-adr-renderer-plugin-design.md` — the v2 spec (600 lines). Architecture is locked.
2. `docs/superpowers/plans/2026-05-20-adr-renderer-plugin-implementation.md` — 28-task TDD implementation plan (3,444 lines). Execute task-by-task.
3. `.superpowers/brainstorm/14592-1779227075/content/adr-interactive-v2.html` — visual mockup. The reference for `templates/styles.css` and `assets/runtime.js` (Tasks 12 and 15 — both invoke `superpowers:frontend-design`).

**Constraints — already decided, do NOT relitigate:**
- Node + unified/remark/rehype pipeline. Not Python, not Pandoc.
- Frontend bundles vendored under `adr/assets/` and inlined into output HTML — no CDN at view time.
- Section recognition is hybrid (canonical headings + frontmatter `sections_override`).
- ADR fields auto-extracted from brainstorming-spec shape with confidence + provenance shown inline. Never fabricate; omit fields rather than guess.
- Top-bar chrome has NO action buttons (just brand + source path + ADR-id/date/status).
- Item-level drill behavior is defined per widget in spec § "Section widget catalog". GENERIC_PROSE + MERMAID_BLOCK explicitly have no drill.
- Templates/CSS authored via the `superpowers:frontend-design` skill — NOT hand-rolled with generic AI conventions.

**State at resume:**
- `docs/` is in `.gitignore` — the spec + plan are working files, not committed. Don't try to commit them.
- No plugin code written yet. Start at Task 1 (scaffold `adr/.claude-plugin/plugin.json`, `package.json`, README, CLAUDE.md, CHANGELOG).
- Branch is `feat/html-adr` (current) but the plan was authored expecting `feat/adr-plugin` — pick one and use it consistently.

**Open question — ask the user first:**
- Execution mode is undecided. The plan ended by offering two paths: **subagent-driven** (dispatch fresh subagent per task, recommended for context isolation) or **inline execution** (this session, batch with checkpoints). Ask before proceeding.

**Do NOT:**
- Skip TDD (each task in the plan is structured test-first → implementation → commit).
- Combine commits across logical changes — the plan specifies one commit per task.
- Push to remote without explicit user confirmation.
